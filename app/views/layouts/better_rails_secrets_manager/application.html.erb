<!DOCTYPE html>
<html>
  <head>
    <title>Rails Secrets Manager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/codemirror.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/theme/monokai.min.css" rel="stylesheet">
    
    <style>
      /* Background Effects */
      .gradient-bg {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      }
      .card-shadow {
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
      }
      
      /* Button Styles - Primary */
      .btn-primary {
        background-color: #4f46e5;
        color: white;
        font-weight: 600;
        padding: 0.5rem 1rem;
        border-radius: 0.375rem;
        transition: all 0.2s;
        display: inline-block;
        text-decoration: none;
        border: none;
        cursor: pointer;
        font-size: 0.875rem;
      }
      .btn-primary:hover {
        background-color: #4338ca;
        transform: translateY(-1px);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      .btn-primary:active {
        transform: translateY(0);
      }
      
      /* Button Styles - Secondary */
      .btn-secondary {
        background-color: #6b7280;
        color: white;
        font-weight: 600;
        padding: 0.5rem 1rem;
        border-radius: 0.375rem;
        transition: all 0.2s;
        display: inline-block;
        text-decoration: none;
        border: none;
        cursor: pointer;
        font-size: 0.875rem;
      }
      .btn-secondary:hover {
        background-color: #4b5563;
        transform: translateY(-1px);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      
      /* Button Styles - Success */
      .btn-success {
        background-color: #10b981;
        color: white;
        font-weight: 600;
        padding: 0.5rem 1rem;
        border-radius: 0.375rem;
        transition: all 0.2s;
        display: inline-block;
        text-decoration: none;
        border: none;
        cursor: pointer;
        font-size: 0.875rem;
      }
      .btn-success:hover {
        background-color: #059669;
        transform: translateY(-1px);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      
      /* Button Styles - Danger */
      .btn-danger {
        background-color: #ef4444;
        color: white;
        font-weight: 600;
        padding: 0.5rem 1rem;
        border-radius: 0.375rem;
        transition: all 0.2s;
        display: inline-block;
        text-decoration: none;
        border: none;
        cursor: pointer;
        font-size: 0.875rem;
      }
      .btn-danger:hover {
        background-color: #dc2626;
        transform: translateY(-1px);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      
      /* Button Styles - Outline */
      .btn-outline {
        background-color: transparent;
        color: #4f46e5;
        font-weight: 600;
        padding: 0.5rem 1rem;
        border: 2px solid #4f46e5;
        border-radius: 0.375rem;
        transition: all 0.2s;
        display: inline-block;
        text-decoration: none;
        cursor: pointer;
        font-size: 0.875rem;
      }
      .btn-outline:hover {
        background-color: #4f46e5;
        color: white;
        transform: translateY(-1px);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      
      /* Link Styles */
      .link-primary {
        color: #4f46e5;
        text-decoration: none;
        transition: color 0.2s;
        font-weight: 500;
      }
      .link-primary:hover {
        color: #4338ca;
        text-decoration: underline;
      }
      
      .link-secondary {
        color: #6b7280;
        text-decoration: none;
        transition: color 0.2s;
      }
      .link-secondary:hover {
        color: #4b5563;
        text-decoration: underline;
      }
      
      /* Environment Button Styles */
      .env-button {
        display: block;
        width: 100%;
        padding: 0.5rem 0.75rem;
        border-radius: 0.375rem;
        text-align: left;
        transition: all 0.2s;
        text-decoration: none;
        font-weight: 500;
      }
      .env-button:hover {
        transform: translateX(2px);
      }
      .env-button-active {
        background-color: #4f46e5;
        color: white;
      }
      .env-button-inactive {
        background-color: #f3f4f6;
        color: #374151;
      }
      .env-button-inactive:hover {
        background-color: #e5e7eb;
      }
      
      /* Form Input Styles */
      input[type="text"], 
      input[type="password"], 
      textarea, 
      select {
        width: 100%;
        padding: 0.5rem 0.75rem;
        border: 1px solid #d1d5db;
        border-radius: 0.375rem;
        font-size: 0.875rem;
        transition: all 0.2s;
      }
      input[type="text"]:focus, 
      input[type="password"]:focus, 
      textarea:focus, 
      select:focus {
        outline: none;
        border-color: #4f46e5;
        box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.1);
      }
      
      /* Icon Buttons */
      .btn-icon {
        padding: 0.5rem;
        border-radius: 0.375rem;
        transition: all 0.2s;
        display: inline-flex;
        align-items: center;
        justify-content: center;
      }
      .btn-icon:hover {
        background-color: #f3f4f6;
      }
      .btn-icon-danger:hover {
        background-color: #fee2e2;
        color: #dc2626;
      }
      
      /* Add Button Styles */
      .btn-add {
        color: #4f46e5;
        font-size: 0.875rem;
        font-weight: 500;
        transition: all 0.2s;
        display: inline-flex;
        align-items: center;
        gap: 0.25rem;
        text-decoration: none;
        padding: 0.25rem 0.5rem;
        border-radius: 0.25rem;
      }
      .btn-add:hover {
        background-color: #eef2ff;
        color: #4338ca;
      }
      
      /* Section Headers */
      .section-header {
        font-size: 1.125rem;
        font-weight: 600;
        color: #374151;
        margin-bottom: 0.75rem;
      }
      
      /* Field Row Styles */
      .field-row {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 0.5rem;
      }
      .field-row input {
        flex: 1;
      }
      
      /* Section Container */
      .section-container {
        border-left: 4px solid #4f46e5;
        padding-left: 1rem;
        margin-bottom: 1.5rem;
      }
      
      /* Alert Animations */
      @keyframes slideDown {
        from {
          transform: translateY(-100%);
          opacity: 0;
        }
        to {
          transform: translateY(0);
          opacity: 1;
        }
      }
      
      .alert {
        animation: slideDown 0.3s ease-out;
      }
    </style>
  </head>

  <body class="bg-gray-50">
    <nav class="gradient-bg shadow-lg">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <h1 class="text-white text-2xl font-bold">Rails Secrets Manager</h1>
          </div>
          <% if session[:authenticated] %>
            <div class="flex items-center space-x-4">
              <% 
                display_env = case current_environment
                when 'credentials'
                  'Main'
                else
                  current_environment.capitalize
                end
              %>
              <span class="text-white">Environment: <strong><%= display_env %></strong></span>
              <%= link_to "Logout", session_path(1), method: :delete, class: "text-white hover:text-gray-200" %>
            </div>
          <% end %>
        </div>
      </div>
    </nav>

    <% if notice.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
          <%= notice %>
        </div>
      </div>
    <% end %>
    
    <% if alert.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/codemirror.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/mode/yaml/yaml.min.js"></script>
  </body>
</html>